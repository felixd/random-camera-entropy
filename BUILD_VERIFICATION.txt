Camera Entropy Distributed v7.8.4 — build verification

Source basis
- Built directly from the complete archive supplied by the user on 2026-08-02.
- No patch installer and no backup copies are included.

Implemented and verified statically
- Continuous USB-agent capture and source warm-up metadata.
- Y8 and packed-LSB frame recorder with 300 GB default limit.
- Safe concurrent dataset reading based on payload-first/index-row commit ordering.
- Relative default DATASET_DIR=data/frame-buffer-latest in both source configurations.
- Dataset preflight in the control server and in run_one.sh.
- Geometry is loaded from manifest.json rather than trusted from the source preset.
- Stopped datasets remain readable.
- start_new_session PID/PGID race removed; API start errors remain JSON.
- Raw frame-buffer directories are excluded from the reports list.

Checks run in the build environment
- Python syntax compilation for all project Python files.
- Bash syntax validation for all project shell scripts.
- Existing core selftest.py.
- New Y8/LSB dataset round-trip test.
- Static and isolated JobManager regression for an immediately exiting child process.

Limitations of build environment
- No physical V4L2 camera, mTLS endpoint, or production data volume was available.
- Flask was not preinstalled and package download was unavailable; control-server behavior was tested with an isolated import shim plus source-level regression checks.
